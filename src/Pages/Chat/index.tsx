import React from 'react';
import { Helmet } from 'react-helmet';

const Chat: React.FC = (): React.ReactElement => {
  return (
    <>
      <Helmet>
        <title>Chatme | message</title>
      </Helmet>
    </>
  );
};

export default Chat;
